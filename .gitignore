# --- Local working folders (not part of the published package) ---
# Project documentation, kept local on purpose.
docs/
# Investigation plans and their board, kept local on purpose.
plans/

# --- Build artifacts: always regenerated by `python -m build` / `pip install -e .` ---
dist/
build/
*.egg-info/

# --- Virtualenv / tooling caches ---
.venv
.pytest_cache/
.ruff_cache/
__pycache__

# --- Editors / OS ---
.idea
.cursor
.vscode
.DS_Store
